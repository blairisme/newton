package helpers;

import org.junit.Test;

public class CommandHelperTest {


    @Test
    public void testCommandExecution(){
      /*  String folderName = "testingdummy123";
        File f = new File(folderName);
        f.mkdir();

        String[] output = LogHelper.executeCmnd("cd "+folderName, true, folderName);

        Assert.assertEquals(output[0], "");
        Assert.assertEquals(output[1], "");
        Assert.assertTrue(new File(LOG_FILENAME).exists());

        output = LogHelper.executeCmnd("ls "+folderName, false, null);

        Assert.assertEquals(output[0].replaceAll("\n", ""), LOG_FILENAME);
        Assert.assertEquals(output[1], "");

        //clean up
        File[] files = f.listFiles();
        if(files!=null) {
            for (File temp : files) {
                temp.delete();
            }
        }
        f.delete();*/


    }
}
