package org.ucl.newton.application.framework;

import org.junit.Assert;
import org.junit.Test;
import org.ucl.newton.framework.Experiment;
import org.ucl.newton.framework.Project;

public class ExperimentTest {

    /*
    @Test
    public void testCreateExperiment(){
        int id = 1;
        String experimentName = "Test experiment";
        Project parent = null;
        Experiment testExperiment = new Experiment(id, experimentName, parent);
        Assert.assertEquals(testExperiment.getId(), 1);
        Assert.assertEquals(testExperiment.getName(), experimentName);
    }
*/

}
